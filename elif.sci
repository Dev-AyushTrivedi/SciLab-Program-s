a = input("enter the number: ")
if(a>=0 && a<10) then
    disp("This is single digit number")
elseif(a>=10 && a<100) then
    disp("This number is double digit No.")
else
    disp("the number id three digit number.")
end
